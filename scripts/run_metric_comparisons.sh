#!/bin/bash
# Compare "quality" metrics of different behavioural embeddings (BE).
# NOTE: Run `run_ubm_experiments.sh` first! This uses data from that run.

python3 run_metric_comparison_experiments.py